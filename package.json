{
  "name": "rbe-ci-red",
  "version": "1.0.0",
  "private": true,
  "description": "Journey fixture: deterministically red CI (deliberate type error). Do not fix on main.",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
